import { Meta, ArgTypes, Description, Title } from "@storybook/blocks";
import { AppProvider } from "./AppProvider";
import * as AppProviderStories from "./AppProvider.stories";

<Meta of={AppProviderStories} />
<Title />
<Description of={AppProvider} />

<ArgTypes of={AppProvider} />
